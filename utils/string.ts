export const doesTextInclude = (text: string, include: string) => text.includes(include);
export const replaceTextWith = (text: string, searchFor: string, replaceWith: string): string => text.replace(searchFor, replaceWith);
